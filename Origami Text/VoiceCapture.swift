// Voice capture, notes, Siri intents, and the note calendar were removed
// on 21 July 2026: notes have moved to the Knowledge Space app, and this
// target is now the letters end of Origami Text (see OrigamiNotesApp.swift).
// The speech/microphone/calendar usage-description build settings on this
// target are dormant and can be deleted in Xcode at leisure. The removed
// code (SpeechRecorder, TranscriptParser, NoteCalendar, VoiceCaptureView,
// OrigamiTextShortcuts) lives on in git history should any of it be wanted
// back.
